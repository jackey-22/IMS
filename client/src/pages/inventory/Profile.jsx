import ProfilePage from "../shared/ProfilePage.jsx";

/**
 * Re-using the premium ProfilePage component for the Inventory Manager's profile route
 * to ensure absolute UI parity and consistency across roles as requested.
 */
export default function Profile() {
  return <ProfilePage />;
}
